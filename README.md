# Currency-Converter
## API Key Required

This project uses the FreeCurrencyAPI. 

To run the program:
1. Create a free account at FreeCurrencyAPI | https://freecurrencyapi.com/
2. Get an API key
3. Rename `config.example.py` to `config.py`
4. Paste your API key into `config.py`
